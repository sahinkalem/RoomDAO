<?xml version="1.0" encoding="utf-8"?>

<resources>

    <style name="ThemeOverlay.SearchView" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@drawable/bg_all_rounded</item>
        <!-- Text color -->
        <item name="android:textColor">@color/black</item>
        <!-- Hint text color -->
        <item name="android:textColorHint">@color/light_grey</item>
        <item name="android:strokeColor">@color/black</item>
        <item name="android:strokeWidth">1</item>
    </style>
</resources>